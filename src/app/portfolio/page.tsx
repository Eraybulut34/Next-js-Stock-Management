import type { Metadata } from 'next';

import { Main } from '@/templates/Main';

export const metadata: Metadata = {
  title: 'Portfolio',
  description: 'Welcome to my portfolio page!',
};

const Portfolio = () => <Main>asdasdsa</Main>;

export default Portfolio;
